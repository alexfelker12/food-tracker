// import { base } from "./base";

// export const profileMiddleware = base.middleware(async ({ context, next, errors }) => {
//   // const sessionData = context.session ?? await auth.api.getSession({
//   //   headers: context.headers,
//   // })

//   // if (!sessionData) throw errors.UNAUTHORIZED()

//   // // Adds session and user to the context
//   // return next({
//   //   context: {
//   //     session: {
//   //       session: sessionData.session,
//   //       user: sessionData.user
//   //     }
//   //   }
//   // })

//   // TODO: add userProfile and/or nutrition result for journal procedures
// })
